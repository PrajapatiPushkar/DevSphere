apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: devsphere-auth-service-hpa
  labels:
    app.kubernetes.io/name: devsphere-auth-service-hpa
    app.kubernetes.io/component: auth-service
    app.kubernetes.io/part-of: devsphere
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: devsphere-auth-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 15
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
      selectPolicy: Min
