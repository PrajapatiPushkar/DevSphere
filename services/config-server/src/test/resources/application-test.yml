server:
  port: 0

spring:
  application:
    name: config-server-test
  cloud:
    config:
      server:
        native:
          search-locations:
            - file:${user.dir}/../../config-repo
            - file:${user.dir}/../config-repo
            - file:${user.dir}/config-repo

management:
  endpoints:
    web:
      exposure:
        include: health
