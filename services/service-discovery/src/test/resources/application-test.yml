server:
  port: 0

spring:
  application:
    name: DEVSPHERE-SERVICE-DISCOVERY-TEST
  config:
    import: ""

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: health
