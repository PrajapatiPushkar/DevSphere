server:
  port: 0

spring:
  application:
    name: DEVSPHERE-SERVICE-DISCOVERY-TEST

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: health
